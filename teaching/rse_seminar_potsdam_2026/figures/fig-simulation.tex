% Logic of simulation as a method -- from pub_rse_methodology/simulation_uml.tex
\documentclass[tikz,border=6pt]{standalone}
\input{tikz-preamble.tex}
\begin{document}
    \begin{tikzpicture}[]

        \node[box](build) {Modell bauen};
        \node[box, below left=of build] (design) {Simulation entwerfen};
        \node[box, below right=of build] (results) {Simulation ausführen};
        \node[box, below=of build] (data) {Beobachtung machen};
        \node[box, below=of design] (assumptions) {Annahmen treffen};

        \node[fork, below=of results] (join_similarities) {};
        \node[fork, below=of assumptions] (join_target) {};
        \node[box, below=of join_similarities] (similarities) {Ähnlichkeit analysieren};
        \node[box, below=of similarities] (knowledge) {publizieren};
        \node[box, below=of join_target] (target) {Zielsystem wählen};
        \node[initial, below=of target] (start) {};
        \node[rsepic, below=of knowledge] (final) {\terminus};

        \draw (start) -- (target);
        \draw (target) -- (join_target);
        \draw (join_target) -- (assumptions);
        \draw (join_target.east) -| (data);
        \draw (assumptions) -- (design);
        \draw (build) -| (results);
        \draw (design) |- (build);
        \draw ($(data.south)+(0.4,0)$) |- (join_similarities.west);
        \draw (results) -- (join_similarities);
        \draw (join_similarities) -- (similarities);
        \draw[uml_activity] (similarities) -- (knowledge);
        \draw (knowledge) -- (final);

    \end{tikzpicture}
\end{document}
